<%= form_tag('product_stats', method: 'post') do |f|%>
  <%= label_tag(:product_id, 'search for product id') %>
  <br/>
  <%= text_field_tag(:product_id) %>
  <br/>
  <%= submit_tag('Submit') %>
<% end %>
